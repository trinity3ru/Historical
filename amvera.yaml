meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  requirementsPath: backend/requirements.txt
  useCache: true

run:
  workdir: backend
  command: uvicorn main:app --host 0.0.0.0 --port 8000
  containerPort: 8000